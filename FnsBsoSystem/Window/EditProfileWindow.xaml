<Window x:Class="FnsBsoSystem.Window.EditProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FnsBsoSystem.Window"
        mc:Ignorable="d"
        Title="Редактирование профиля" Height="580" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- ЦВЕТА -->
        <Color x:Key="ColorPrimary">#009CDF</Color>
        <SolidColorBrush x:Key="BrushPrimary" Color="{StaticResource ColorPrimary}"/>
        <SolidColorBrush x:Key="BrushLabel" Color="#888"/>

        <!-- СТИЛЬ LABEL -->
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="{StaticResource BrushLabel}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10,0,0,5"/>
        </Style>

        <!-- СТИЛЬ INPUT WRAPPER (Капсула) -->
        <Style TargetType="Border" x:Key="InputWrapper">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Background" Value="#F0F2F5"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- СТИЛЬ TEXTBOX -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <!-- СТИЛЬ КНОПКИ -->
        <Style TargetType="Button" x:Key="BtnSave">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="25" Background="{StaticResource BrushPrimary}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ОСНОВНАЯ КАРТОЧКА ОКНА -->
    <Border Background="White" CornerRadius="20" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Шапка -->
                <RowDefinition Height="*"/>
                <!-- Поля -->
                <RowDefinition Height="Auto"/>
                <!-- Кнопка -->
            </Grid.RowDefinitions>

            <!-- 1. ШАПКА -->
            <Grid Grid.Row="0" Margin="0,0,0,25">
                <StackPanel>
                    <TextBlock Text="РЕДАКТИРОВАНИЕ" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushPrimary}"/>
                    <TextBlock Text="Личные данные сотрудника" FontSize="12" Foreground="#999"/>
                </StackPanel>

                <!-- Кнопка закрытия (Крестик) -->
                <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Click="BtnClose_Click" Cursor="Hand"
                        Background="Transparent" BorderThickness="0">
                    <Path Data="M0,0 L10,10 M10,0 L0,10" Stroke="#CCC" StrokeThickness="2"/>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="Transparent" Width="30" Height="30">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- 2. ПОЛЯ ВВОДА -->
            <StackPanel Grid.Row="1">
                <!-- Фамилия -->
                <TextBlock Text="Фамилия" Style="{StaticResource LabelStyle}"/>
                <Border Style="{StaticResource InputWrapper}">
                    <TextBox x:Name="TxtLast"/>
                </Border>

                <!-- Имя -->
                <TextBlock Text="Имя" Style="{StaticResource LabelStyle}"/>
                <Border Style="{StaticResource InputWrapper}">
                    <TextBox x:Name="TxtFirst"/>
                </Border>

                <!-- Отчество -->
                <TextBlock Text="Отчество" Style="{StaticResource LabelStyle}"/>
                <Border Style="{StaticResource InputWrapper}">
                    <TextBox x:Name="TxtMiddle"/>
                </Border>

                <!-- Отдел (ComboBox) -->
                <TextBlock Text="Отдел" Style="{StaticResource LabelStyle}"/>
                <Border Style="{StaticResource InputWrapper}">
                    <ComboBox x:Name="ComboDept" 
                              DisplayMemberPath="DeptName" SelectedValuePath="Id"
                              Background="Transparent" BorderThickness="0"
                              VerticalContentAlignment="Center" Padding="10,0" Height="40"/>
                </Border>

                <!-- Должность (ComboBox) -->
                <TextBlock Text="Должность" Style="{StaticResource LabelStyle}"/>
                <Border Style="{StaticResource InputWrapper}">
                    <ComboBox x:Name="ComboPos" 
                              DisplayMemberPath="PosName" SelectedValuePath="Id"
                              Background="Transparent" BorderThickness="0"
                              VerticalContentAlignment="Center" Padding="10,0" Height="40"/>
                </Border>
            </StackPanel>

            <!-- 3. КНОПКА СОХРАНИТЬ -->
            <Button Grid.Row="2" Content="Сохранить изменения" 
                    Click="BtnSave_Click"
                    Style="{StaticResource BtnSave}" Height="50" Margin="0,10,0,0"/>
        </Grid>
    </Border>
</Window>
