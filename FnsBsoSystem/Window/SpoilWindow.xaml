<Window x:Class="FnsBsoSystem.SpoilWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown" Width="450" SizeToContent="Height">

    <Window.Resources>
        <!-- Обновленная палитра -->
        <Color x:Key="ColorDanger">#D32F2F</Color>
        <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource ColorDanger}"/>
        <SolidColorBrush x:Key="BrushBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BrushText" Color="#333333"/>

        <!-- Стиль полей ввода (с эффектом Border) -->
        <Style TargetType="TextBox" x:Key="ModernInput">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource BrushDanger}"/>
                    <Setter Property="Background" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Основной контейнер -->
    <Border Background="{StaticResource BrushBackground}" CornerRadius="16" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="5" Opacity="0.15"/>
        </Border.Effect>

        <StackPanel Margin="30">
            <!-- Заголовок -->
            <Grid Margin="0,0,0,20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Списание документа" FontSize="20" FontWeight="Bold" Foreground="{StaticResource BrushText}"/>
                    <TextBlock Text="Операция необратима" FontSize="13" Foreground="#999"/>
                </StackPanel>

                <Button Content="✕" Click="BtnClose_Click" HorizontalAlignment="Right" 
                        Background="Transparent" BorderThickness="0" FontSize="16" Foreground="#999" 
                        Cursor="Hand" VerticalAlignment="Top"/>
            </Grid>

            <!-- Поля -->
            <TextBlock Text="Номер бланка" FontSize="12" FontWeight="SemiBold" Foreground="#666" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtNum" Style="{StaticResource ModernInput}" Height="40" Margin="0,0,0,20"/>

            <TextBlock Text="Причина списания" FontSize="12" FontWeight="SemiBold" Foreground="#666" Margin="0,0,0,5"/>
            <ComboBox x:Name="ComboReason" Height="40" Margin="0,0,0,20" Padding="10,0">
                <ComboBoxItem Content="Технический брак" IsSelected="True"/>
                <ComboBoxItem Content="Ошибка заполнения"/>
                <ComboBoxItem Content="Утеря"/>
            </ComboBox>

            <!-- Инфо-блок -->
            <Border Background="#FFF5F5" CornerRadius="8" Padding="12" Margin="0,0,0,25" BorderBrush="#FFCDD2" BorderThickness="1">
                <TextBlock Text="Внимание: удаление данных невозможно отменить." 
                           FontSize="12" Foreground="{StaticResource BrushDanger}" TextWrapping="Wrap"/>
            </Border>

            <!-- Кнопка -->
            <Button Content="УНИЧТОЖИТЬ" Click="Save" Height="45" Cursor="Hand"
                    Background="{StaticResource BrushDanger}" Foreground="White" 
                    FontWeight="Bold" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Border>
</Window>