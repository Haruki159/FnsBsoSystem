<Page x:Class="FnsBsoSystem.Page.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FnsBsoSystem.Page"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
       Title="История" Background="White">

    <Page.Resources>
        <!-- ЦВЕТА -->
        <Color x:Key="ColorPrimary">#009CDF</Color>
        <SolidColorBrush x:Key="BrushPrimary" Color="{StaticResource ColorPrimary}"/>
        <SolidColorBrush x:Key="BrushText" Color="#444444"/>

        <!-- СТИЛЬ ЗАГОЛОВКОВ КОЛОНОК (Синяя шапка) -->
        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{StaticResource BrushPrimary}" BorderThickness="0,0,1,0" BorderBrush="#33FFFFFF">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ СТРОК (Подсветка при наведении) -->
        <Style x:Key="RowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#EEEEEE"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F9FF"/>
                    <!-- Светло-голубой при наведении -->
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D6F0FF"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- СТИЛЬ ЯЧЕЕК (Убираем синюю обводку фокуса) -->
        <Style x:Key="CellStyle" TargetType="DataGridCell">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border BorderThickness="0" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center" Margin="15,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="40,20,40,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="*"/>
            <!-- Таблица -->
        </Grid.RowDefinitions>

        <!-- 1. ЗАГОЛОВОК СТРАНИЦЫ -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="ЖУРНАЛ ОПЕРАЦИЙ" 
                       FontSize="28" FontWeight="Bold" Foreground="#333"/>
            <TextBlock Text="История действий пользователей в системе" 
                       FontSize="14" Foreground="#888" Margin="2,5,0,0"/>

            <!-- Декоративная линия -->
            <Border Width="50" Height="4" Background="{StaticResource BrushPrimary}" HorizontalAlignment="Left" CornerRadius="2" Margin="0,15,0,0"/>
        </StackPanel>

        <!-- 2. ТАБЛИЦА В "КАРТОЧКЕ" -->
        <Border Grid.Row="1" Background="White" CornerRadius="15" BorderBrush="#E0E0E0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.05" ShadowDepth="5"/>
            </Border.Effect>

            <!-- Обрезаем углы таблицы, чтобы они не вылезали за Border -->
            <Border CornerRadius="15" ClipToBounds="True">
                <DataGrid x:Name="GridHistory" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F0F0F0"
                          BorderThickness="0"
                          Background="White"
                          ColumnHeaderStyle="{StaticResource HeaderStyle}"
                          RowStyle="{StaticResource RowStyle}"
                          CellStyle="{StaticResource CellStyle}">

                    <DataGrid.Columns>
                        <!-- Дата -->
                        <DataGridTextColumn Header="Дата и время" Binding="{Binding OperationDate, StringFormat='dd.MM.yyyy  |  HH:mm'}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#555"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Пользователь -->
                        <DataGridTextColumn Header="Пользователь" Binding="{Binding UserName}" Width="200">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource BrushPrimary}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Действие -->
                        <DataGridTextColumn Header="Действие" Binding="{Binding ActionType}" Width="200"/>

                        <!-- Подробности -->
                        <DataGridTextColumn Header="Подробности операции" Binding="{Binding Details}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Border>
    </Grid>
</Page>
